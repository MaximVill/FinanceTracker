<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.financetracker.ui.MainController"
      spacing="10">
    <padding><Insets topRightBottomLeft="20"/></padding>

    <!-- верхняя панель с балансом -->
    <HBox spacing="20">
        <VBox>
            <Label fx:id="balanceLabel" text="Баланс: 0.00" style="-fx-font-size: 24; -fx-font-weight: bold;"/>
            <HBox spacing="10">
                <Label fx:id="incomeLabel" text="Доходы: +0.00" style="-fx-text-fill: green;"/>
                <Label fx:id="expenseLabel" text="Расходы: -0.00" style="-fx-text-fill: red;"/>
            </HBox>
        </VBox>
        <VBox spacing="5">
            <Button text="🔄 Обновить курсы" onAction="#handleRefreshRates"/>
            <Button text="Сменить валюту" onAction="#handleChangeCurrency"/>
            <Button text="Очистить всё" onAction="#handleClearAll"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white;"/>
        </VBox>
    </HBox>

    <!-- сообщение об ошибке -->
    <Label fx:id="errorLabel" style="-fx-text-fill: red; -fx-font-weight: bold;" visible="false"/>

    <!-- кнопки добавления транзакций -->
    <HBox spacing="20" alignment="CENTER">
        <Button text="➕ Добавить доход" onAction="#handleAddIncome"
                style="-fx-background-color: #2ecc71; -fx-text-fill: white; -fx-font-size: 16; -fx-padding: 15 30;"/>
        <Button text="➖ Добавить расход" onAction="#handleAddExpense"
                style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 16; -fx-padding: 15 30;"/>
    </HBox>

    <!-- таблица транзакций -->
    <TableView fx:id="transactionsTable" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="titleColumn" text="Название" prefWidth="200"/>
            <TableColumn fx:id="amountColumn" text="Сумма" prefWidth="100"/>
            <TableColumn fx:id="currencyColumn" text="Валюта" prefWidth="80"/>
            <TableColumn fx:id="categoryColumn" text="Категория" prefWidth="150"/>
            <TableColumn fx:id="typeColumn" text="Тип" prefWidth="80"/>
            <TableColumn fx:id="dateColumn" text="Дата" prefWidth="100"/>
            <TableColumn fx:id="actionsColumn" text="Действия" prefWidth="120"/>
        </columns>
    </TableView>
</VBox>